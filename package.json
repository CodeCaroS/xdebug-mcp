{
  "name": "xdebug-mcp",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "bin": {
    "xdebug-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "tsc -p tsconfig.test.json && node --test test-dist/test/*.test.js",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0-beta.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
